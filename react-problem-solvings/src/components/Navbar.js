import { Link } from "react-router-dom";
const Navbar = () => {
  retrun(
    <div>
      <Link> Home</Link>
      <Link>About</Link>
      <Link>Contact</Link>
    </div>
  );
};

export default Navbar;
